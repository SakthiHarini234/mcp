Model Context Protocol:
It is used to connect AI agents with external tools and data through standardized protocol.
MCP Host – AI Application. It is responsible for running AI experience.
MCP Client – Component inside host that communicates with MCP server.
MCP Server – This is where capabilities are exposed.
Tool – Does something
Resource – Provide info/data.
MCP can also expose reusable prompt templates.
MCP Inspector – Testing tool for MCP server.

Practical Learning:
Created simple MCP Calculator and based on that I have created MCP server, tools and I have also run the MCP Inspector and also tested MCP tools.

References:
https://modelcontextprotocol.io/docs/2026-07-28/getting-started/intro
https://www.geeksforgeeks.org/artificial-intelligence/model-context-protocol-mcp/

03-09-2026
MCP Client - It connects AI application to MCP Server. It discovers available tools and call required tools.
LLM Integration - Integrated LLM with MCP to understand the user's request and select required MCP tools.

Practical Learning:
Created an MCP Client for the Calculator MCP Server and tested list of tools and call tools. Integrated Groq LLM with MCP. The LLM can select the required calculator tool based on the user's question, the MCP Client executes the tool, and the result is sent back to the LLM.

Flow:
User - Groq LLM - MCP Client - MCP Server - Tool - Result - LLM - Final Response.

04-09-2026
MCP Resources - Provide info or data to MCP Client. It is used to list and read available resources.
MCP Prompts - It is a reusable prompt template used to generate specific instructions for LLM
Database Integration - Connected a SQLite database with the MCP Server to store calculator expressions and results.

Practical Learning:
Created MCP Resources for calculator help and stored calculations. Created an MCP Prompt to explain calculations step by step. Connected a SQLite database and read the stored calculation data through an MCP resource

Flow:
MCP Client(Ask info) - MCP Server(Gives requested resource or prompt) - Resource/Prompt(Gives required info) - MCP Client(Receive info)

05-09-2026
Agent Loop = LLM decides - Tool executes - Result goes back to LLM - Final answer.
Multiple Tool Calls - Allows the agent to use multiple tools for one task.
Sequential Execution - Executes tools one by one using the previous result.
Conditional Execution - LLM decides which tool is needed based on the task.
Context Management - Keeps previous messages, tool calls, and results.
Error Recovery - Handles tool errors and sends the error back to the LLM.
Multi-Step Agent - Completes a task through multiple steps using MCP tools.
Human Approval - User can approve or reject a tool before it executes.

Practical Learning:
Created an MCP Agent using Groq LLM and MCP Client. Implemented an agent loop that allows the LLM to decide when to use MCP tools. Executed multiple tools sequentially, maintained context between tool calls, handled tool execution errors, and added human approval before executing sensitive tools.

Flow:
                       ┌───────────────┐
                       │     User      │
                       └───────┬───────┘
                               ↓
                       ┌───────────────┐
                       │    Groq LLM   │
                       └───────┬───────┘
                               ↓
                       Need MCP Tool?
                         ↙           ↘
                       No             Yes
                       ↓               ↓
                 Final Answer    Human Approval?
                                    ↙      ↘
                                  No        Yes
                                  ↓          ↓
                               Cancel    MCP Client
                                             ↓
                                        MCP Server
                                             ↓
                                           Tool
                                             ↓
                                          Result
                                             ↓
                                      Update Context
                                             ↓
                                          LLM
                                             ↓
                                    Another Tool?
                                      ↙       ↘
                                    Yes        No
                                     ↓          ↓
                                  Tool Loop  Final Answer
                                  
07-09-2026
JSON-RPC - Used for communication between MCP Client and MCP Server using requests and responses.
MCP Lifecycle - Defines how the MCP Client connects, initializes, communicates, and ends the session with the Server.
Capability Negotiation - Allows the Client and Server to identify the features supported by each other.
Notifications - Used to send updates without requiring a response.
Pagination - Divides large data into smaller pages for easier processing.
Cancellation - Allows a running task to be cancelled before it finishes.
Progress - Used to track and show the progress of a long-running task.
Logging - Used to monitor application activity, warnings, and errors.

Practical Learning:
Built a basic JSON-RPC communication flow and explored the MCP lifecycle and capability negotiation. Implemented examples for notifications, pagination, cancellation, and progress tracking. Also explored MCP logging and learned how to handle the current SDK's logging deprecation using application-level logging.

Flow:
MCP Client - MCP Server - MCP Protocol - Request/Response or Notification - Result/Update - MCP Client

08-09-2026
STDIO Transport - Used for local communication between the MCP Client and MCP Server through standard input and output.
Streamable HTTP - Used to communicate between the MCP Client and MCP Server over HTTP.
HTTP MCP Server - Created an MCP Server that runs through Streamable HTTP and exposes tools, resources, and prompts.
Remote MCP Client - Connected an MCP Client to a remote MCP Server using an HTTP endpoint.
Basic Authentication - Implemented Bearer Token authentication to verify the MCP Client before allowing access to the MCP Server.
Environment Variables - Used environment variables to configure transport, host, port, and authentication token without hardcoding values.
Error Handling - Used exception handling to manage MCP connection, authentication, and communication errors.
Real-World MCP Server - Created a practical calculator MCP Server with tools, a resource, and a prompt.

Practical Learning:
Built an MCP Client and Server using STDIO and Streamable HTTP. Implemented remote communication, Bearer Token authentication, environment based configuration, error handling, and a real-world calculator server with tools, resources and prompts.

